const express=require("express"),c=require("../controllers/taskController"),protect=require("../middleware/auth"),validate=require("../middleware/validate"),r=express.Router();r.use(protect);r.get("/project/:projectId",c.list);r.post("/project/:projectId",validate({title:{required:true,type:"string",minLength:2,maxLength:200}}),c.create);r.put("/:id",c.update);r.patch("/:id/move",validate({status:{required:true,type:"string"}}),c.move);r.delete("/:id",c.remove);module.exports=r;