const jwt=require("jsonwebtoken"),User=require("../models/User");module.exports=async(req,res,next)=>{try{const h=req.headers.authorization;if(!h?.startsWith("Bearer "))return res.status(401).json({success:false,message:"Authentication required"});const d=jwt.verify(h.slice(7),process.env.JWT_SECRET),u=await User.findById(d.id).select("-password");if(!u)return res.status(401).json({success:false,message:"User no longer exists"});req.user=u;next()}catch(e){if(["JsonWebTokenError","TokenExpiredError"].includes(e.name))return res.status(401).json({success:false,message:"Invalid or expired token"});next(e)}};